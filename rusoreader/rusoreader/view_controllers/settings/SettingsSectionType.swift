/// In the settings we will have several standard types of cells, we will use this enum to handle what will happen when cell appears in table and when it is clicked.
enum SettingsSectionType {
    case pickerWheel
}
